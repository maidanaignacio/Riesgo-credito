# Riesgo-credito
Sistema de predicción de riesgo crediticio con SQL Server y Machine Learning
# Estructura profesional para Data Science
mkdir -p {data,sql,python,notebooks,dashboards,docs,models,scripts}

# Crea archivos esenciales
touch README.md .gitignore requirements.txt

# Estructura SQL Server
mkdir -p sql/{schemas,procedures,queries,data_import}

# Estructura Python/ML
mkdir -p python/{data_processing,model_training,model_evaluation,api}
